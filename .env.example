# Copy to .env. deploy.sh writes this file for you if it is missing.

# Signs the player cookie. Anything else means every restart hands out new ids.
#   openssl rand -hex 16
BJ_SESSION_KEY=

# Host port the table is served on.
PORT=8090
